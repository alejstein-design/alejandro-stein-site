# Sanity CMS — required
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity API token — needed for write operations (optional for read-only)
SANITY_API_TOKEN=

# Site URL — used for sitemap and OG metadata
NEXT_PUBLIC_SITE_URL=https://alejandrostein.com
